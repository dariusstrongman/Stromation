Start a local HTTP server to preview the Stromation website in the browser.

Run `python -m http.server 8000` in the project root directory and tell the user to open http://localhost:8000 in their browser. Run it in the background so the conversation can continue.
