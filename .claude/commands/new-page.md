Create a new page for the Stromation website.

Page details: $ARGUMENTS

Steps:
1. Read `about.html` as a template for the standard page structure (head, nav, footer, scripts)
2. Create the new HTML file in the project root with:
   - Proper SEO meta tags matching the page topic
   - Open Graph and Twitter card meta tags
   - Canonical URL
   - The standard nav header (with no `active` class on any link, or on the correct one if it's being added to nav)
   - The standard footer
   - Page content based on the user's description
3. Add the new page to `sitemap.xml` with today's date
4. Ask the user if they want the page added to the navigation bar
